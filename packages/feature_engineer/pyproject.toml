[tool.poetry]
name = "feature_engineer"
version = "1.0.0"
description = "Rolling z-score feature engineering indicators for Project AEGIS"
authors = ["Antigravity <antigravity@google.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
pandas = "^2.2.0"
numpy = "^1.26.3"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
